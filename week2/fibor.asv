function f = fibor(n)
    if n <= 2
        f = 1;
    else
        f = [fibor(n-1) fib(n)];
    end
end

function f2 = fib(n)

    if n <= 2
        f2 = 1;
    else 
        f2 = fib(n - 2) + fib(n - 1);
    end
end